# EchoAce

Full stack built with React, Next.js, Supabase, and Express

Install dependencies with `yarn`. Start the frontend with `yarn workspace frontend dev` and the backend with `yarn workspace backend dev`.
